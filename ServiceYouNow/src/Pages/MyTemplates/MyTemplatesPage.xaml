<Page x:Class="ServiceYouNow.src.pages.mytemplates.MyTemplatesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:local="clr-namespace:ServiceYouNow.src.pages.mytemplates"
      mc:Ignorable="d"
      Background="White">


	<Grid>
		<Button
			Content="←"
			FontSize="20"
			Background="#585958"
			Foreground="White"
			HorizontalAlignment="Left"
			VerticalAlignment="Top"
			Margin="20"
			Width="50"
			Height="30"
			Click="BackButton_Click" />

		<ListView x:Name="TemplatesListView"
				  Margin="20,70,20,20"
				  HorizontalAlignment="Stretch"
				  VerticalAlignment="Stretch">
			<ListView.View>
				<GridView>
					<GridViewColumn Header="Request Name" Width="200" DisplayMemberBinding="{Binding Name}" />
					<GridViewColumn Header="Environment" Width="100" DisplayMemberBinding="{Binding Environment}" />
					<GridViewColumn Header="Server" Width="150" DisplayMemberBinding="{Binding ServerName}" />
					<GridViewColumn Header="Access Level" Width="100" DisplayMemberBinding="{Binding AccessLevel}" />
				</GridView>
			</ListView.View>
		</ListView>
	</Grid>


</Page>
